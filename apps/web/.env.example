FORKFOLIO_API_BASE_URL=http://localhost:8000
FORKFOLIO_API_BASE_PATH=/api/v1
# Optional: required if backend auth token middleware is enabled.
FORKFOLIO_API_TOKEN=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional but recommended in production: canonical web origin used for auth callback redirects.
FORKFOLIO_APP_ORIGIN=
