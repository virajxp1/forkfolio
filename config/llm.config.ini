[llm]
# Default LLM model used for OpenRouter requests.
model_name = openai/gpt-oss-20b

[braintrust]
project = ForkFolio

[embeddings]
# Embedding model for vector search (OpenRouter). Pricing: $0.005 per 1M input tokens.
model_name = baai/bge-base-en-v1.5
