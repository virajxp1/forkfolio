[dedupe]
# Embedding type used for nearest-neighbor lookup.
embedding_type = title_ingredients

# If cosine distance is <= this value, treat as duplicate immediately.
strict_duplicate_threshold = 0.05

# If cosine distance is > this value, treat as distinct (skip LLM check).
distance_threshold = 0.30
